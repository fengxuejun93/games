<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.companymanagement.mapper.PermissionMapper">
    <select id="selectByRoleIds" resultType="com.example.companymanagement.entity.Permission">
        SELECT DISTINCT p.* FROM permission p
        JOIN role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
        ORDER BY p.parent_id, p.sort
    </select>
    
    <select id="selectByRoleId" resultType="com.example.companymanagement.entity.Permission">
        SELECT p.* FROM permission p
        JOIN role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId}
        ORDER BY p.parent_id, p.sort
    </select>
</mapper>