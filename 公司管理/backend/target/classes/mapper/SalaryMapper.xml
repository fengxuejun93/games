<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.companymanagement.mapper.SalaryMapper">
    <select id="selectByCompanyIdAndMonth" resultType="com.example.companymanagement.entity.Salary">
        SELECT * FROM salary 
        WHERE company_id = #{companyId} 
          AND salary_month = #{salaryMonth}
        ORDER BY created_at DESC
    </select>
    
    <select id="selectByEmployeeId" resultType="com.example.companymanagement.entity.Salary">
        SELECT * FROM salary 
        WHERE employee_id = #{employeeId}
        ORDER BY salary_month DESC
    </select>
    
    <select id="selectByEmployeeIdAndMonth" resultType="com.example.companymanagement.entity.Salary">
        SELECT * FROM salary 
        WHERE employee_id = #{employeeId} 
          AND salary_month = #{salaryMonth}
    </select>
</mapper>